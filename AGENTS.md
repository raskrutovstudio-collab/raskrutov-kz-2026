# Обязательные правила проекта

Перед изменениями прочитать `PROJECT_PROFILE.md`, применимые файлы `.cursor/rules/` и навык `.cursor/skills/build-static-seo-page/SKILL.md`.

1. Сначала проверить `git status`, структуру проекта и существующие компоненты. Сохранять пользовательские изменения.
2. Использовать существующий визуальный язык и проверенный шаблон нужного типа страницы.
3. Проектировать mobile-first. Для новой, пилотной или существенно изменённой страницы проверять 360, 390, 412, 430 и 1280 px. Для массовой региональной генерации на одном утверждённом HTML/CSS-шаблоне viewport QA выполнять по `.cursor/rules/027-regional-viewport-qa.mdc`. Если DOM, CSS или layout страницы отличается от эталона — полный расширенный QA обязателен.
4. Контент первого экрана и H1 должны быть доступны из HTML без ожидания JavaScript.
5. Не ставить `loading="lazy"` на изображения первого экрана. Только вероятный LCP получает `fetchpriority="high"`.
6. Все изображения получают размеры; адаптивные изображения — `srcset`/`sizes`; ниже первого экрана — lazy-loading.
7. Не добавлять библиотеку, если задача решается текущим vanilla JavaScript.
8. Не выдумывать факты, цены, отзывы, рейтинги и реквизиты. Не размечать невидимое содержимое.
9. Писать прямо и уверенно, без риторических противопоставлений и сравнительных шаблонов.
10. После изменений выполнить `npm run quality:all`, проверить страницу визуально, консоль, формы и показать `git diff`. Для regional batch требования evidence и критерии PASS определяются `027-regional-viewport-qa.mdc`; общий visual smoke не может понизить этот gate.
11. Работать только в машинной ветке: PC1 — `work/pc1`, PC2 — `work/pc2`. Не переключаться на `main` для редактирования.
12. В обычной Cursor-сессии commit/push выполняет after-response hook автоматически после `quality:all`. Не выполнять ручной push в `main`.
13. GitHub Actions последовательно интегрирует машинные ветки в `main`. При изменении одного и того же файла двумя ПК интеграция останавливается для ручной проверки.

